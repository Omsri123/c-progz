#include<stdio.h>
main()
{
	int n,s,i;
	float m ;
	printf("enter total numbers to calculate \n");
	scanf("%d",&n);
	int mean[n];
	for(i=0,s=0;i<n;i++)
	{
		printf("enter %d number \n",i+1);
		scanf("%d",&mean[i]);
		s=s+mean[i];
	}
	m=s/n;
	printf("mean is %f",m);
}
