#include<stdio.h>
main()

{
	float l,b,p,a;;
	//get length//
	printf("enter length");
	scanf("%f",&l);
	//get breadth//
	printf("enter breadth");
	scanf("%f",&b);
	
	p=2*(l+b);
	a=l*b;
	
	printf("perimeter of rectangle is %f and\n area of rectangle is %f",p,a);
	
}
